import React,{useEffect,useState} from 'react';
import {ShoppingBag,Menu,Plus,Minus,Trash2,Users,ClipboardList,LogOut,Star,Palette,ChevronLeft,CheckCircle2,Phone,MapPin,Clock,Edit3,X,Tags,Image as ImageIcon,Bell,Home as HomeIcon,Utensils,Layers,Tag,Gift,LocateFixed,Search,UserRound,MapPinned,Download,BarChart3,LogOut as CustomerLogOut} from 'lucide-react';
import {useProgress} from '../../context/ProgressContext';
import {api,money,applyTheme,updateSectionMeta} from '../../lib/utils';
import TrackingResultInline from '../shared/TrackingResultInline';
import TrackOrder from './TrackOrder';
import CustomerPanel from './CustomerPanel';
import CartModal from './CartModal';
import CustomizationModal from './CustomizationModal';
import TrackingModal from './TrackingModal';
import SuccessModal from './SuccessModal';
import Footer from './Footer';
export default function Home(){const {run}=useProgress();const [data,setData]=useState(null),[cart,setCart]=useState([]),[selectedCat,setSelectedCat]=useState('all'),[search,setSearch]=useState(''),[checkout,setCheckout]=useState(false),[customer,setCustomer]=useState(null),[accountOpen,setAccountOpen]=useState(false),[sent,setSent]=useState(null),[tracking,setTracking]=useState(null),[trackingOpen,setTrackingOpen]=useState(false),[activeSection,setActiveSection]=useState('home'),[scrolled,setScrolled]=useState(false),[addedNotice,setAddedNotice]=useState(null),[viewMode,setViewMode]=useState('grid'),[loading,setLoading]=useState(true),[error,setError]=useState(''),[customizationItem,setCustomizationItem]=useState(null),[mobileMenuOpen,setMobileMenuOpen]=useState(false);useEffect(()=>{let active=true;api('/store').then(d=>{if(!active)return;setData(d);applyTheme(d.settings)}).catch(e=>setError(e.message||'تعذر تحميل الموقع')).finally(()=>setLoading(false));return()=>{active=false}},[]);useEffect(()=>{api('/customer/me').then(setCustomer).catch(()=>setCustomer(null))},[]);useEffect(()=>{if(!data)return;updateSectionMeta(data.settings);const onHash=()=>updateSectionMeta(data.settings);window.addEventListener('hashchange',onHash);const onScroll=()=>setScrolled(window.scrollY>18);onScroll();window.addEventListener('scroll',onScroll,{passive:true});const ids=['home','categories','offers','menu','new-items','full-menu','contact'];const observer=new IntersectionObserver(entries=>{const visible=entries.filter(e=>e.isIntersecting).sort((a,b)=>b.intersectionRatio-a.intersectionRatio)[0];if(visible)setActiveSection(visible.target.id)}, {rootMargin:'-92px 0px -55% 0px',threshold:[0.1,0.25,0.5]});ids.map(id=>document.getElementById(id)).filter(Boolean).forEach(el=>observer.observe(el));return()=>{window.removeEventListener('hashchange',onHash);window.removeEventListener('scroll',onScroll);observer.disconnect()}},[data]);const total=cart.reduce((a,x)=>a+x.price*x.quantity,0);const cartCount=cart.reduce((a,x)=>a+x.quantity,0);const showAdded=(name)=>{setAddedNotice(name);window.clearTimeout(window.__sajAddedTimer);window.__sajAddedTimer=window.setTimeout(()=>setAddedNotice(null),2600)};const add=i=>{let opts=[];try{opts=JSON.parse(i.options_json||'[]')}catch{};if(opts.length){setCustomizationItem({...i,options:opts});return}addWithOptions(i,[])};const addWithOptions=(i,chosen)=>run('جاري إضافة الصنف…',async()=>{const extra=chosen.reduce((sum,n)=>sum+Number((i._options||[]).find(o=>o.name===n)?.price||0),0),key=`${i.id}|${chosen.join(',')}`;setCart(c=>{const old=c.find(x=>(x.cartKey||x.itemId)===key);return old?c.map(x=>(x.cartKey||x.itemId)===key?{...x,quantity:x.quantity+1}:x):[...c,{itemId:Number(i.id),cartKey:key,productId:Number(i.id),name:i.name,price:Number(i.price)+extra,quantity:1,options:chosen}]});showAdded(i.name)});
const addOffer=o=>run('جاري إضافة العرض…',async()=>{const key=`offer-${o.id}`;setCart(c=>{const old=c.find(x=>x.itemId===key);return old?c.map(x=>x.itemId===key?{...x,quantity:x.quantity+1}:x):[...c,{itemId:key,offerId:Number(o.id),name:o.title,price:Number(o.price),quantity:1}]});showAdded(o.title)});if(loading)return <div className="loader">جاري التحميل…</div>;if(error)return <div className="loader"><div><b>تعذر تحميل الموقع</b><p>{error}</p><button className="btn primary" onClick={()=>location.reload()}>إعادة المحاولة</button></div></div>;if(!data)return <div className="loader">لا توجد بيانات للموقع.</div>;const categories=Array.isArray(data.categories)?data.categories:[];const allItems=Array.isArray(data.items)?data.items.filter(i=>i.available!==false):[];const items=allItems.filter(i=>(selectedCat==='all'||String(i.category_id)===selectedCat)&&(!search||`${i.name} ${i.description||''} ${i.allergens||''}`.toLowerCase().includes(search.toLowerCase())));const offers=Array.isArray(data.offers)?data.offers:[];const categoryImage=c=>{if(String(c.name||'').trim()==='العروض'){const latestOffer=offers.filter(o=>o.active!==false&&o.image).slice().sort((a,b)=>Number(b.id)-Number(a.id))[0];return latestOffer?.image||allItems.find(i=>Number(i.category_id)===Number(c.id)&&i.image)?.image}return allItems.find(i=>Number(i.category_id)===Number(c.id)&&i.image)?.image};const featured=allItems.filter(i=>i.featured).slice(0,4);const popular=(featured.length?featured:allItems).slice(0,4);const newest=allItems.filter(i=>!featured.some(f=>Number(f.id)===Number(i.id))).slice(-4).reverse();const submitSearch=e=>{e?.preventDefault();if(search.trim()){setSelectedCat('all');requestAnimationFrame(()=>document.getElementById('full-menu')?.scrollIntoView({behavior:'smooth',block:'start'}))}};return <div className="site-shell">
<header className={`${scrolled?'is-scrolled':''} ${mobileMenuOpen?'mobile-menu-active':''}`}><div className="nav">
<a className="brand" href="#home"><span className="brand-logo">{data.settings.logoUrl?<img src={data.settings.logoUrl} alt="شعار الموقع"/>:<span className="logo-mark">ف</span>}</span><span><b>{data.settings.siteName}</b><small>{data.settings.tagline||'مناقيش صاج طازة على كيفك'}</small></span></a>
<nav className={mobileMenuOpen?'mobile-nav-open':''} aria-label="التنقل الرئيسي"><a className={activeSection==='home'?'active':''} href="#home" onClick={()=>window.setTimeout(()=>setMobileMenuOpen(false),220)}><HomeIcon size={15}/> <span>الرئيسية</span></a><a className={['menu','full-menu'].includes(activeSection)?'active':''} href="#full-menu" onClick={()=>window.setTimeout(()=>setMobileMenuOpen(false),220)}><Utensils size={15}/> <span>قائمتنا</span></a><a className={activeSection==='categories'?'active':''} href="#categories" onClick={()=>window.setTimeout(()=>setMobileMenuOpen(false),220)}><Layers size={15}/> <span>الأقسام</span></a><button className="nav-link-button nav-track-button" onClick={()=>{setTrackingOpen(true);setTracking(null);window.setTimeout(()=>setMobileMenuOpen(false),220)}}><LocateFixed size={15}/> <span>تتبع الطلب</span></button></nav>
<div className="nav-actions"><button className="account-btn" onClick={()=>setAccountOpen(true)}><UserRound size={16}/> {customer?.user?.name||'حسابي'}</button><a className="nav-call" href={`tel:${data.settings.phone||''}`}><Phone size={16}/> اتصل بنا</a><button className="nav-order" onClick={()=>document.getElementById('full-menu').scrollIntoView({behavior:'smooth'})}>اطلب الآن</button></div>
<button className={`cart-btn ${cartCount?'has-items':''}`} onClick={()=>setCheckout(true)} aria-label={`السلة${cartCount?`، ${cartCount} أصناف`:''}`}><ShoppingBag size={19}/><span>السلة</span>{cartCount>0&&<em>{cartCount}</em>}</button><button type="button" className="mobile-menu-toggle" onClick={()=>setMobileMenuOpen(v=>!v)} aria-label={mobileMenuOpen?'إغلاق القائمة':'فتح القائمة'} aria-expanded={mobileMenuOpen}>{mobileMenuOpen?<X size={22}/>:<Menu size={22}/>}</button>
</div></header>{mobileMenuOpen&&<button className="mobile-nav-backdrop" aria-label="إغلاق القائمة" onClick={()=>window.setTimeout(()=>setMobileMenuOpen(false),220)} />}
<main>
<section id="home" className="hero hero-compact-home"><div className="hero-compact-inner"><div className="hero-compact-brand"><div><h1>{data.settings.siteName}</h1><p>{data.settings.tagline||'مناقيش صاج طازة على كيفك'}</p></div></div><div className="news-ticker news-ticker-compact"><span className="news-label"><Bell size={12}/> جديدنا</span><div className="news-track"><div className="news-content">{newest.slice(0,3).map((i,n)=><span className="news-item" key={`new-${i.id}-${n}`}><b>{i.name}</b>{i.description&&<small>{i.description}</small>}</span>)}</div></div></div></div></section>
<div className="home-layout">
  <aside id="categories" className="home-categories"><div className="home-categories-title"><span className="eyebrow">تشكيلتنا</span><h2>الأقسام</h2></div><div className="home-category-list"><button className={selectedCat==='all'?'home-category active':'home-category'} onClick={()=>setSelectedCat('all')}><Utensils size={17}/><span>الكل</span></button>{categories.map(c=><button key={c.id} className={selectedCat===String(c.id)?'home-category active':'home-category'} onClick={()=>setSelectedCat(String(c.id))}>{categoryImage(c)?<img src={categoryImage(c)} alt=""/>:<span className="home-category-icon">🥙</span>}<span>{c.name}</span></button>)}</div></aside>
  <div className="home-content">
    <section className="home-search-section"><form className="menu-search home-search" role="search" onSubmit={submitSearch}><Search size={20}/><input value={search} onChange={e=>setSearch(e.target.value)} placeholder="ابحث عن صنف، مكوّن أو وصف…" aria-label="البحث عن صنف أو مكوّن أو وصف"/><button type="button" className="search-clear" onClick={()=>setSearch('')} aria-label="مسح البحث">{search?<X size={18}/>:null}</button><button type="submit" className="search-submit">بحث</button></form></section>
    <section id="menu" className="section compact-section home-popular-section"><div className="section-head"><div><span className="eyebrow">اختياراتنا</span><h2>الأكثر طلباً</h2></div><span className="muted">{popular.length} أصناف</span></div><div className="popular-grid">{popular.map(i=><article className={`popular-item product-glass-card ${cart.some(x=>x.itemId===i.id)?'is-selected':''}`} key={i.id}><div className="popular-image">{i.image?<img src={i.image} alt={i.name}/>:<div className="food-placeholder">🥙</div>}</div><div className="popular-body product-glass-body">{i.featured&&<span className="mini-badge">مميز</span>}<h3>{i.name}</h3><p>{i.description}</p>{i.allergens&&<small className="allergen-note">⚠ مسببات الحساسية: {i.allergens}</small>}<div className="item-foot"><strong>{money(i.price,data.settings.currency)}</strong><button className={`add ${cart.some(x=>x.itemId===i.id)?'selected-add':''}`} aria-label={`إضافة ${i.name} إلى السلة`} onClick={()=>add(i)}>{cart.some(x=>x.itemId===i.id)?<CheckCircle2 size={17}/>:<Plus size={17}/>}</button></div></div></article>)}</div></section>
    <section id="full-menu" className="section compact-section home-menu-section"><div className="section-head"><div><span className="eyebrow">قائمتنا</span><h2>{selectedCat==='all'?'قائمتنا':categories.find(c=>String(c.id)===selectedCat)?.name||'قائمتنا'}</h2></div><span className="muted">{search.trim()?`${items.length} نتيجة بحث`:`${items.length} صنف`}</span></div><div className="menu-view-toolbar" role="group" aria-label="طريقة عرض الأصناف"><button type="button" className={`view-toggle ${viewMode==='grid'?'active':''}`} aria-pressed={viewMode==='grid'} onClick={()=>setViewMode('grid')}><span aria-hidden="true">▦</span> الشبكة</button><button type="button" className={`view-toggle ${viewMode==='table'?'active':''}`} aria-pressed={viewMode==='table'} onClick={()=>setViewMode('table')}><span aria-hidden="true">☷</span> الجدول</button></div>{viewMode==='grid'?(<div className="menu-grid">{items.map(i=><article className={`menu-item-card product-glass-card ${cart.some(x=>x.itemId===i.id)?'is-selected':''}`} key={i.id}><div className="menu-item-image">{i.image?<img src={i.image} alt={i.name}/>:<div className="food-placeholder">🥙</div>}</div><div className="menu-item-body product-glass-body"><button className="favorite-btn" onClick={async()=>{if(!customer){setAccountOpen(true);return}try{const r=await api('/customer/favorites/'+i.id,{method:'POST'});setCustomer(c=>({...c,favorites:r.favorite?[...(c.favorites||[]),Number(i.id)]:(c.favorites||[]).filter(x=>x!==Number(i.id))}))}catch(e){alert(e.message)}}} title="المفضلة">{customer?.favorites?.includes(Number(i.id))?'♥':'♡'}</button>{i.featured&&<span className="mini-badge">مميز</span>}<h3>{i.name}</h3><p>{i.description}</p>{i.allergens&&<small className="allergen-note">⚠ مسببات الحساسية: {i.allergens}</small>}<div className="item-foot"><strong>{money(i.price,data.settings.currency)}</strong><button className={`add ${cart.some(x=>x.itemId===i.id)?'selected-add':''}`} aria-label={`إضافة ${i.name} إلى السلة`} onClick={()=>add(i)}>{cart.some(x=>x.itemId===i.id)?<CheckCircle2 size={17}/>:<Plus size={17}/>}</button></div></div></article>)}</div>):(<div className="menu-table-wrap" role="region" aria-label="جدول الأصناف" tabIndex="0"><table className="menu-table"><thead><tr><th scope="col">التصنيف</th><th scope="col">الصورة</th><th scope="col">الاسم</th><th scope="col">الوصف</th><th scope="col">السعر</th><th scope="col">إضافة</th></tr></thead><tbody>{items.map(i=><tr key={i.id}><td>{categories.find(c=>Number(c.id)===Number(i.category_id))?.name||'—'}</td><td>{i.image?<img src={i.image} alt="" loading="lazy"/>:<span className="menu-table-no-image">—</span>}</td><td><strong>{i.name}</strong></td><td>{i.description||'—'}</td><td><strong>{money(i.price,data.settings.currency)}</strong></td><td><button type="button" className={`add ${cart.some(x=>x.itemId===i.id)?'selected-add':''}`} aria-label={`إضافة ${i.name} إلى السلة`} onClick={()=>add(i)}>{cart.some(x=>x.itemId===i.id)?<CheckCircle2 size={17}/>:<Plus size={17}/>}</button></td></tr>)}</tbody></table></div>)}{items.length===0&&<div className="empty-state">لا توجد أصناف متاحة في هذا التصنيف حالياً.</div>}</section>
  </div>
</div>
<section id="orders" className="info-strip"><div><Clock/><b>طازج يومياً</b><span>تحضير حسب الطلب</span></div><div><MapPin/><b>استلام / توصيل</b><span>حسب منطقتك</span></div><div><Phone/><b>تواصل معنا</b><span>{data.settings.phone||'أضف رقم الهاتف من الإدارة'}</span></div></section>
</main><Footer settings={data.settings}/>{addedNotice&&<div className="cart-toast" role="status" aria-live="polite"><CheckCircle2 size={20}/><div><b>تمت الإضافة إلى السلة</b><span>{addedNotice}</span></div><button onClick={()=>setCheckout(true)}>عرض السلة ({cartCount})</button></div>}{trackingOpen&&<div className="modal-bg tracking-overlay"><div className="modal tracking-search-modal"><button className="close" onClick={()=>{setTrackingOpen(false);setTracking(null)}}><X/></button><span className="eyebrow">متابعة</span><h2>تتبع طلبك</h2><TrackOrder onResult={setTracking}/>{tracking&&<TrackingResultInline result={tracking}/>}</div></div>}{accountOpen&&<CustomerPanel customer={customer} setCustomer={setCustomer} onClose={()=>setAccountOpen(false)} onReorder={o=>{setCart(o.items||[]);setAccountOpen(false);setCheckout(true)}}/>}{customizationItem&&<CustomizationModal item={customizationItem} currency={data.settings.currency} onClose={()=>setCustomizationItem(null)} onConfirm={chosen=>{setCustomizationItem(null);addWithOptions({...customizationItem,_options:customizationItem.options},chosen)}}/>}{checkout&&<CartModal cart={cart} setCart={setCart} total={total} settings={data.settings} customer={customer} onClose={()=>setCheckout(false)} onSent={x=>{setSent(x);setCheckout(false);setCart([]);if(x?.loyaltyBalance!=null)setCustomer(c=>c?({...c,points:Number(x.loyaltyBalance)}):c)}}/>}{sent&&<SuccessModal result={sent} phone={data.settings.phone} onClose={()=>setSent(null)}/>}</div>}
