import React,{useState} from 'react';
import {useProgress} from '../../context/ProgressContext';
import {api} from '../../lib/utils';
export default function Login({onLogin}){const {run}=useProgress();const [email,setEmail]=useState(''),[password,setPassword]=useState(''),[err,setErr]=useState(''),[busy,setBusy]=useState(false),[forgot,setForgot]=useState(false);const submit=async e=>{e.preventDefault();setErr('');setBusy(true);try{await run('جاري تسجيل الدخول…',()=>api('/auth/login',{method:'POST',body:JSON.stringify({email,password})}));onLogin()}catch(e){setErr(e.message)}finally{setBusy(false)}};const request=async e=>{e.preventDefault();setErr('');try{const r=await run('جاري إرسال الطلب…',()=>api('/auth/forgot-password',{method:'POST',body:JSON.stringify({email})}));setErr(r.message||'تم إرسال الطلب إلى المدير.')}catch(e){setErr(e.message)}};return <div className="login-page"><div className="login-card"><div className="logo-mark big">ف</div>{forgot?<><span className="eyebrow">استعادة الحساب</span><h1>نسيت كلمة المرور؟</h1><p>أدخل بريدك الإلكتروني لإرسال طلب إلى المدير للموافقة على إعادة التعيين.</p><form onSubmit={request}><input type="email" required placeholder="البريد الإلكتروني" value={email} onChange={e=>setEmail(e.target.value)}/>{err&&<div className="error">{err}</div>}<button className="btn primary full">إرسال طلب إعادة التعيين</button></form><button className="btn ghost full" onClick={()=>{setErr('');setForgot(false)}}>العودة لتسجيل الدخول</button></>:<><h1>لوحة إدارة فرنية صاج</h1><p>سجّل الدخول لإدارة الموقع.</p><form onSubmit={submit}><input type="email" required placeholder="البريد الإلكتروني" value={email} onChange={e=>setEmail(e.target.value)}/><input type="password" required placeholder="كلمة المرور" value={password} onChange={e=>setPassword(e.target.value)}/>{err&&<div className="error">{err}</div>}<button className="btn primary full" disabled={busy}>{busy?'جاري الدخول…':'دخول'}</button></form><button className="forgot-link" onClick={()=>{setErr('');setForgot(true)}}>نسيت كلمة المرور؟</button><a href="/" className="back">← العودة للموقع</a></>}</div></div>}
