export default function Footer({settings}){const hasLocation=Number.isFinite(Number(settings.latitude))&&Number.isFinite(Number(settings.longitude));return <footer id="contact"><div className="footer-main"><div className="footer-brand"><b>{settings.siteName}</b>{settings.tagline&&<span>{settings.tagline}</span>}</div><div className="footer-contact">{settings.address&&<div><span>العنوان</span><b>{settings.address}</b></div>}{settings.phone&&<div><span>الهاتف</span><a dir="ltr" href={`tel:${settings.phone}`}>{settings.phone}</a></div>}{hasLocation&&<a className="footer-location" href={`https://www.openstreetmap.org/?mlat=${encodeURIComponent(settings.latitude)}&mlon=${encodeURIComponent(settings.longitude)}#map=17/${encodeURIComponent(settings.latitude)}/${encodeURIComponent(settings.longitude)}`} target="_blank" rel="noreferrer">عرض الموقع على الخريطة</a>}</div></div><small>© {new Date().getFullYear()} جميع الحقوق محفوظة</small></footer>}
