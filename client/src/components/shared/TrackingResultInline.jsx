import {money,moneyInWords} from '../../lib/utils';
export default ({result})=>{const statuses={new:'جديد',confirmed:'مؤكد',preparing:'قيد التحضير',ready:'جاهز',delivered:'تم التسليم',cancelled:'ملغى'};const steps=['new','confirmed','preparing','ready','delivered'];const orderStatus=result.status||'new';const current=steps.indexOf(orderStatus);return <div className="tracking-result-inline"><div className="tracking-result-head"><span>الطلب #{result.id}</span><b>{statuses[orderStatus]||orderStatus}</b></div><div className="tracking-steps">{steps.map((s,i)=><div key={s} className={`tracking-step ${orderStatus==='cancelled'?'cancelled ':''}${i<=current&&orderStatus!=='cancelled'?'done':''}`}><span>{i+1}</span><b>{statuses[s]}</b></div>)}</div>{result.estimatedReadyAt&&<div className="tracking-eta">الوقت التقديري للجهوزية: {new Date(result.estimatedReadyAt).toLocaleTimeString('ar-LB-u-nu-latn',{hour:'2-digit',minute:'2-digit'})}</div>}<div className="tracking-summary"><span>المجموع</span><div className="total-values"><b>{money(result.total,result.currency||'ل.ل')}</b><small>{moneyInWords(result.total,result.currency||'ل.ل')}</small></div></div></div>}
