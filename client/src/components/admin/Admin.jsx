import React,{useEffect,useRef,useState} from 'react';
import {Bell,ClipboardList,Tags,Palette,Gift,BarChart3,Users,LogOut,Menu,X} from 'lucide-react';
import {api,money,moneyInWords} from '../../lib/utils';
import {useProgress} from '../../context/ProgressContext';
import SideButton from './SideButton';
import Orders from './Orders';
import Items from './Items';
import SettingsPanel from './SettingsPanel';
import AnalyticsPanel from './AnalyticsPanel';
import MarketingPanel from './MarketingPanel';
import UsersPanel from './UsersPanel';
import CustomersPanel from './CustomersPanel';
export default function Admin(){const [data,setData]=useState(null),[tab,setTab]=useState('orders'),[me,setMe]=useState(null),[newOrderNotice,setNewOrderNotice]=useState(null),[mobileMenuOpen,setMobileMenuOpen]=useState(false);const previousOrderIds=useRef(null);const noticeTimer=useRef(null);const load=async({notify=true}={})=>{try{const next=await api('/admin/data');const currentOrders=Array.isArray(next.orders)?next.orders:[];if(notify&&previousOrderIds.current){const previous=previousOrderIds.current;const added=currentOrders.find(o=>!previous.has(Number(o.id)));if(added){setNewOrderNotice(added);if(noticeTimer.current)clearTimeout(noticeTimer.current);noticeTimer.current=setTimeout(()=>setNewOrderNotice(null),6000)}}previousOrderIds.current=new Set(currentOrders.map(o=>Number(o.id)));setData(next)}catch(e){location.reload()}};useEffect(()=>{let active=true;api('/auth/me').then(x=>{if(active)setMe(x.user)}).catch(()=>{});load({notify:false});const timer=setInterval(()=>load({notify:true}),10000);return()=>{active=false;clearInterval(timer);if(noticeTimer.current)clearTimeout(noticeTimer.current)}} ,[]);if(!data)return <div className="loader">جاري تحميل الإدارة…</div>;const can=p=>me?.role==='admin'||me?.permissions?.includes(p);const newOrdersCount=data.orders.filter(o=>o.status==='new').length;const resetRequestsCount=(data.resetRequests||[]).length;return <div className={`admin ${mobileMenuOpen?"mobile-menu-open":""}`}>{newOrderNotice&&<div className="new-order-toast" role="alert"><div className="new-order-toast-icon"><Bell size={20}/></div><div><b>طلب جديد #{newOrderNotice.id}</b><span>{newOrderNotice.customer_name||'زبون جديد'} — {money(newOrderNotice.total,data.settings.currency)}</span><small>{moneyInWords(newOrderNotice.total,data.settings.currency)}</small></div><button type="button" onClick={()=>{setTab('orders');setNewOrderNotice(null)}}>عرض الطلب</button></div>}<button type="button" className="admin-mobile-menu-toggle" onClick={()=>setMobileMenuOpen(v=>!v)} aria-label={mobileMenuOpen?'إغلاق القائمة':'فتح القائمة'} aria-expanded={mobileMenuOpen}>{mobileMenuOpen?<X size={22}/>:<Menu size={22}/>}</button>{mobileMenuOpen&&<button type="button" className="admin-drawer-backdrop" aria-label="إغلاق القائمة" onClick={()=>setMobileMenuOpen(false)} />}<aside><div className="admin-brand"><span className="logo-mark">ف</span><div><b>فرنية صاج</b><small>لوحة الإدارة</small></div></div><div className="admin-nav"><SideButton icon={<ClipboardList/>} label="الطلبات" badge={newOrdersCount} active={tab==='orders'} onClick={()=>{setTab('orders');setMobileMenuOpen(false)}} show={can('RECEIVE_ORDERS')}/><SideButton icon={<Tags/>} label="الأصناف والتصنيفات" active={tab==='items'} onClick={()=>{setTab('items');setMobileMenuOpen(false)}} show={can('MANAGE_ITEMS')}/><SideButton icon={<Palette/>} label="إدارة الموقع" active={tab==='settings'} onClick={()=>{setTab('settings');setMobileMenuOpen(false)}} show={can('MANAGE_SETTINGS')}/><SideButton icon={<Gift/>} label="التوصيل والتسويق" active={tab==='marketing'} onClick={()=>{setTab('marketing');setMobileMenuOpen(false)}} show={can('MANAGE_ITEMS')}/><SideButton icon={<BarChart3/>} label="الإحصاءات" active={tab==='analytics'} onClick={()=>{setTab('analytics');setMobileMenuOpen(false)}} show={can('RECEIVE_ORDERS')}/><SideButton icon={<Users/>} label="المستخدمون" badge={resetRequestsCount} active={tab==='users'} onClick={()=>{setTab('users');setMobileMenuOpen(false)}} show={can('MANAGE_USERS')}/><SideButton icon={<Users/>} label="الزبائن" active={tab==='customers'} onClick={()=>{setTab('customers');setMobileMenuOpen(false)}} show={can('MANAGE_USERS')}/></div><button className="logout" onClick={async()=>{try{await api('/auth/logout',{method:'POST'})}finally{location.reload()}}}><LogOut/> خروج</button></aside><main className="admin-main"><div className="admin-top"><div><span className="eyebrow">مرحباً {me?.name}</span><h1>{tab==='orders'?'الطلبات':tab==='items'?'الأصناف والتصنيفات والعروض':tab==='settings'?'إدارة الموقع':tab==='analytics'?'الإحصاءات':tab==='customers'?'الزبائن':'مستخدمو الإدارة'}</h1></div><a className="view-site" href="/">مشاهدة الموقع ↗</a></div>{tab==='orders'&&<Orders data={data} refresh={()=>load({notify:false})}/>} {tab==='items'&&<Items data={data} refresh={()=>load({notify:false})}/>} {tab==='settings'&&<SettingsPanel data={data} refresh={()=>load({notify:false})}/>} {tab==='analytics'&&<AnalyticsPanel/>} {tab==='marketing'&&<MarketingPanel data={data} refresh={()=>load({notify:false})}/>} {tab==='users'&&<UsersPanel data={data} refresh={()=>load({notify:false})} canAddAdmin={can('ADD_ADMIN')}/>} {tab==='customers'&&<CustomersPanel data={data} refresh={()=>load({notify:false})}/>}</main></div>}
